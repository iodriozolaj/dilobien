/**
 * Supabase connection config for the /core page.
 * Edit ONLY the two values below (from Supabase: Project Settings -> API).
 * This is the public "anon" key — it is meant to be exposed in client code,
 * as long as Row Level Security only allows INSERT (see schema.sql).
 */
window.SUPABASE_CONFIG = {
  url: "https://kxzrmaanynpclctrtiyb.supabase.co",
  anonKey: "sb_publishable_s9bIB36Z0CsyPfy1Vht0tQ_vzl6To7S"
};
